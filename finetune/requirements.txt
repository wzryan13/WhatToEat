# 微调子项目依赖 —— 装进本 worktree 的独立 .venv，勿与主项目 .venv 混用
# 环境：Python 3.13 / Apple Silicon (MPS) / 无 CUDA（不要装 bitsandbytes）

# 训练
torch>=2.4
transformers>=4.46
peft>=0.13
datasets>=3.0
accelerate>=1.1
trl>=0.12
pyyaml>=6.0

# teacher 打标签（复用主项目 query_understanding，需要调用 DeepSeek）
langchain-deepseek>=0.1.3
python-dotenv>=1.0
